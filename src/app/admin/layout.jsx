import React from 'react';

export const metadata = {
  title: 'Admin Login | Mickey Crackers',
  robots: 'noindex, nofollow',
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
