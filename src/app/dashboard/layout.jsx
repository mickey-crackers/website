import React from 'react';

export const metadata = {
  title: 'Admin Dashboard | Mickey Crackers',
  robots: 'noindex, nofollow',
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
